const urls = {
    GetBaseURL: () => {
        // return process.env.NODE_ENV == 'development' ? urls.BASE_PROD: urls.BASE_DEV;
        return 'https://www-iranintl-com.translate.goog/finance?_x_tr_sl=en&_x_tr_tl=fa&_x_tr_hl=en&_x_tr_pto=wapp';
    },
}

export default urls;